# SnapWheel v0.3.0

**构建方式**：普通构建（完整版）

## 本版定义（按你的划分）
> **这些功能真正实现的版本** → v0.3.0

## 相较 v0.2.0 新增 / 修正
### 1. 万能键（摇杆式圆盘）
- 位于**四分之一弧的内侧中点**：**92px 半透明大圆盘**，带内圈与四向小点，不是小按钮感。
- **长按左键 >0.26s** → 展开**四扇区圆盘**（曲线缓动）。
- 按住拖到扇区松手执行：**上=新建 Wheel ｜ 右=下一个 ｜ 下=删除 ｜ 左=上一个**。
- **删除两步确认**：拖到「删除」变红进入确认态，再拖到「确认」松手才删除。
- 下方显示当前 Wheel 名称；圆盘位置与缩略图、角上按钮均不重叠。

### 2. 框选缩放修正（重要）
- **修复“缩放时上边跟着动 / 一下飞走”**：原因是缩放后错误地做了“外接圆夹取”，把整个选区往里推，导致固定角位移；锁定比例时更明显。
- 现在：**对角绝对固定**，只按比例限制尺寸保证不出屏；**锁定比例时缩放稳定**。

### 3. 其它
- **缩略图拖拽判定修正**：命中改为“按实际绘制尺寸”（所见即所得），拖动阈值 6px → 10px。
- 框选支持：角点缩放、连体锁定键、旋转键旋转输出。

## 文件
- `SnapWheel.exe` —— 完整版可执行文件（含万能键）
- `SnapWheel.cs` —— 完整源码（同一源码用 `NO_KEY` 开关可分别构建 0.2.0 / 0.3.0）
- `snapwheel.ico` —— 图标

## 编译
```
C:\Windows\Microsoft.NET\Framework64\v4.0.30319\csc.exe /nologo /optimize+ /target:winexe /win32icon:snapwheel.ico /out:SnapWheel.exe SnapWheel.cs
```
