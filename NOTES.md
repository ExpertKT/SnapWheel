# SnapWheel v0.3.9

**日期**：2026-09-12

## 本次更新
- **万能键质感重做**：外发光 + 球面径向渐变 + 顶部玻璃高光弧 + 边缘细线，按下时轻微放大变亮（带缓动）
- **修掉重叠**：图片计数标签挪到环外侧 45° 对角线上，彻底离开万能键和角上按钮；改成深色药丸 + 主题色圆点 + 细描边
- **Wheel 名标签重做**：原来是硬邦邦一块黑底；现在是圆角药丸 + 主题色圆点 + 细描边，位置也离开万能键一点
- **修设置界面按钮显示 bug**：确定/取消原来靠一个 250px 假占位控件硬顶（AutoSize 一算就错位/被裁），改成四列表格右对齐；RoundButton 也先铺父容器底色再画圆角，四角不再有脏底。新增「新手引导」按钮
- **长按放大倍数可调**：设置里「长按放大(%)」，默认 240
- **首次打开自动弹新手引导**（6 条要点 + 管理员权限小提示），看过就不再弹；托盘/设置里都能再叫出来
- **开启动画**：整条环像彩虹一样扫出来 → 图片一张张沿弧线排队滑落 → 万能键/三个按钮/Wheel 名/计数标签从屏幕外滑入并渐显；设置里可关，托盘可重播
- 设置窗口新增「启动时播放开启动画」开关