# SnapWheel v0.4.5

**日期**：2026-09-12

## 本次更新
- **修复 v0.4.4 引入的两个毛玻璃问题**（都是缩放改造时的坐标疏漏）：
  - 模糊背景是物理像素尺寸、绘制却在 UiK 缩放后的逻辑坐标系里，没除回去 —— 高 DPI（比如 125%）下玻璃底被放大又偏移，整块玻璃就废了。现在按逻辑尺寸贴回去
  - 模糊背景没乘淡出 alpha —— 淡出时玻璃底不跟着变淡，面板像卡住一样不消失。现在每个玻璃面都把自己的 alpha 传进去（含抽屉式的黑底加暗）
- 新增淡出回归测试：同一批关键像素在 _show=1.00 / 0.50 / 0.25 下的平均 alpha 必须逐级下降（实测 244 → 168 → 98）