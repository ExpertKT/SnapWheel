## v0.7.0

**截图浮层新增两个工具**

| 新功能 | 怎么用 |
|---|---|
| **贴 emoji** | 工具条上的笑脸按钮 → 弹出常用表情面板（表情 / 手势 / 符号三组）→ 点一个就插到选区里。插进去的 emoji **和箭头、文字一样是标注元素**：能拖动、能滚轮/按钮改大小、能 `Del` 删除、`Ctrl+Z` 撤销，确认时合成进图。用 GDI 绘制，所以是 **Windows 的彩色 emoji**（GDI+ 会画成黑白） |
| **另存为** | 工具条上的存盘按钮 → 系统保存对话框，把**当前框选区域连同标注**存到你选的位置；格式按扩展名走（PNG / JPEG / BMP，JPEG 自动铺白底避免透明变黑）。存完之后这张图**仍然留在轮盘里**，方便继续拖出去用 |

**界面语言（第一轮 i18n）**

- 设置 → 行为 → **界面语言**：`跟随系统 / 中文 / English`，**切换后重启生效**
- 默认**跟随系统语言**（中文系统中文界面、其它系统英文界面），也可以显式指定、之后就固定
- 第一轮已接入：**托盘菜单全部 14 项** + 设置里这个选项本身；其余界面（设置页标签、引导页、对话框、浮层工具条、各类提示）留到第二轮
- 实现方式是 `Lang.T("中文", "English")` 就地双写，不做 key 字典 —— 漏翻一眼能看见

**英文物料与项目页**

- README 改为**英文为主、中文附后**，标题：`"Maybe the best screenshot tool out there" — SnapWheel：快照轮环`
- 仓库简介同步、补齐 topics（screenshot / screen-capture / ocr / winforms / portable…）
- 新增英文竖版图文生成器与英文发帖文案（Reddit / Product Hunt / HN 三版语气）

**修掉的问题**

- **拖出缩略图后不再从环上移除**：拖出是 Copy 语义，以前当成 Move，导致"拖一次就没了、不能再拖"。现在默认留一份，可在设置第 1 页关掉
- **轮盘置顶改为不抢焦点**（`SetWindowPos` + `SWP_NOACTIVATE`）：以前每 2 秒用 `TopMost` 重设一次会抢前台，导致托盘右键菜单和打赏窗口"弹出来过一会儿自己关了"
- 置顶校验加了条件：显式设为非置顶时（打开打赏 / 设置 / 引导）不再把它拽回最顶层
- 反馈里的系统版本改读注册表（以前固定报 `Windows NT 6.2` = Win8）
- 缩略图删除后视口平滑跟进、滚动范围放开到最后一张、计数序号改成弧下端那一张（1..总数）
- 大图缩略图的放大动画首次不再卡（源码退用缓存里最大的小图）

---
# 更新日志

## 关于版本编号

同一份源码用编译开关产出**两条线**：

| 版本线 | 编译方式 | 含义 |
|---|---|---|
| **v0.2.x** | `csc /define:NO_KEY` | **没有万能键**，但有框选缩放 + 比例锁定 + 多 Wheel |
| **v0.3.x / v0.4.x** | 普通编译 | **完整版**，额外含**万能键（摇杆圆盘）** |

两条线功能同步，编号并行推进（例如 v0.4.6 ↔ v0.2.16）。

每个版本的完整快照（源码 + exe + 图标 + 说明）都在 [`versions/`](versions/) 目录里存档。

---

## 已发布 Release（里程碑版本）

只挑选**功能里程碑、且没有致命 bug** 的版本做成 Release；其余版本仅作为记录存档在 `versions/`。

| 版本 | 日期线 | 为什么是里程碑 |
|---|---|---|
| [v0.1.1](versions/v0.1.1/) | 起点 | 首个可用版本：快照轮环的核心形态 |
| [v0.2.0](versions/v0.2.0/) | 无万能键线起点 | 引入多 Wheel + 框选缩放 / 比例锁定 / 旋转 |
| [v0.3.0](versions/v0.3.0/) | 完整线起点 | 加入**万能键摇杆圆盘**，缩放改为对角固定 |
| [v0.3.3](versions/v0.3.3/) | 修复 | 缩放「飞走」根因（缺被拖角方向符号），四个角终于都能正常拖 |
| [v0.3.5](versions/v0.3.5/) | 修复 | 拖图崩溃修复 + **全局异常兜底**，成为第一个稳定基准 |
| [v0.3.7](versions/v0.3.7/) | 修复 | 拖放命中根因：分层窗口按 alpha 命中测试 → 加不可见「接住区」 |
| [v0.3.9](versions/v0.3.9/) | 功能 | 万能键质感重做 + **开启动画** + **新手引导** |
| [v0.4.0](versions/v0.4.0/) | 大版本 | **UI 全面改版：新拟态 + 扁平化 + 毛玻璃**，设置新增完整「风格」自定义 |
| [v0.4.5](versions/v0.4.5/) | 修复 | 修掉 v0.4.4 引入的毛玻璃错位与淡出失效 |
| [v0.4.6](versions/v0.4.6/) | 修复 | 按钮四角脏块根治 + 玻璃更透 + 对话框实底 |
| [v0.2.16](versions/v0.2.16/) | 无万能键线 | 与 v0.4.6 功能同步的无万能键版本 |
| [v0.4.7](versions/v0.4.7/) | 手感 | 九轮交互打磨：万能键长按提速、关闭键「后悔」机制、动画曲线、正式更名「快照轮环」 |

> ⚠️ **不建议使用的版本**（保留仅为记录）：
> - **v0.2.4 / v0.3.4**：拖图高亮颜色分量越界，一拖图就弹「未处理异常」把程序打死 —— v0.3.5 已修
> - **v0.2.14 / v0.4.4**：缩放改造导致毛玻璃被放大偏移、淡出动画失效（"毛玻璃全毁"）—— v0.4.5 已修

---

## 完整版本记录

<details>
<summary>展开全部 48 个版本</summary>

| 版本 | 说明 |
|---|---|
| v0.1.1 | 首个正式版本：快照轮环完整功能 |
| v0.2.0 | **（无万能键线）** 多 Wheel + 框选缩放/比例锁定 + 旋转键 |
| v0.3.0 | **（完整线）** v0.2.0 全部 + 万能键摇杆圆盘（弧内侧中点）+ 缩放修正（对角固定） |
| v0.2.1 | 修复缩放「只能拖一点」与「乱飞」；新增右上角**宽高输入 + 角度归零**（无万能键） |
| v0.3.1 | 同 v0.2.1 的全部修复与新增，并包含**万能键**（完整版） |
| v0.2.2 | **（无万能键线）** 修复缩放贴边「回弹抽搐」（先把鼠标点夹进屏幕再算尺寸） |
| v0.3.2 | **（完整线）** v0.2.2 的缩放修复；万能键「删除」改为**摇杆左右两半确认**：左半=取消（绿）、右半=确认删除（红），鼠标所在半高亮，点别处或 10 秒自动取消 |
| v0.2.3 | **（无万能键线）** 修复四角缩放符号 bug（3 个角会崩/飞）、旋转后出屏收紧、最近角抓取 |
| v0.3.3 | **（完整线）** v0.2.3 全部修复 —— 缩放「飞走」根因是缺了被拖角的方向符号（su/sv） |
| v0.2.4 | **（无万能键线）** 支持把外部图片拖进轮盘 + 全格式加载 + 智能存盘 + 托盘导入 ⚠️ 含崩溃 bug |
| v0.3.4 | **（完整线）** 同上：拖图进轮盘（含文件夹）、png/jpg/**ico**/gif/tif/webp/heic/avif/jxl/RAW 全格式 ⚠️ 含崩溃 bug |
| v0.2.5 | **（无万能键线）** 修复拖图高亮色越界导致的崩溃 + 全局异常兜底 |
| v0.3.5 | **（完整线）** 紧急修复崩溃（颜色分量越界）；新增全局异常兜底（写 error.log + 托盘气泡，不再弹框退出）+ 绘制逐帧 try-catch |
| v0.2.6 | **（无万能键线）** 拖放改宽松：整窗接收 + 位图拖入 + 拖回带滑入动画 |
| v0.3.6 | **（完整线）** 拖图进轮盘**整窗范围都接收**、拖拽时不点穿、支持直接拖位图、拖回重播滑入动画；新增 19 项拖放回归测试 |
| v0.2.7 | **（无万能键线）** 不可见「接住区」修复拖放命中 + 管理员身份提示 |
| v0.3.7 | **（完整线）** 根因修复：分层窗口按 alpha 命中测试（采样命中 28/400 → 105/400）；环带+摇杆键铺 alpha=1 不可见接住区；管理员身份检测与「以普通权限重启」 |
| v0.2.8 | **（无万能键线）** 极端比例阈值收紧到 4.5:1 |
| v0.3.8 | **（完整线）** 极端比例判定 2.8:1 → **4.5:1**，阈值做成常量 `WheelForm.ExtremeRatio`；补 9 项比例判定用例 |
| v0.2.9 | **（无万能键线）** 万能键质感重做 + 计数标签去重叠 + Wheel 名标签重做 + 设置按钮修复 + 长按放大可调 + 开启动画 + 新手引导 |
| v0.3.9 | **（完整线）** 同上；新增 UI 截图工具 `tests/ui-shot.cs` |
| v0.2.10 | **（无万能键线）** 同 v0.4.0 的 UI 改版与风格设置 |
| v0.4.0 | **（完整线）** **UI 全面改版：新拟态 + 扁平化 + 毛玻璃**；设置新增「风格」栏（界面风格/主题色/玻璃不透明度/圆角/阴影/动画速度/标签开关）；设置窗口改两栏；新增 48 组风格组合绘制测试 |
| v0.2.11 | **（无万能键线）** 同 v0.4.1：轮盘真毛玻璃 + 托盘开启动画 + 万能键悬停 + 点名字改名 |
| v0.4.1 | **（完整线）** **轮盘上的真·毛玻璃**（抓屏 + 盒式模糊做面板背景）；万能键悬停反馈；点名字药丸可直接改名 |
| v0.2.12 | **（无万能键线）** 同 v0.4.2：修复改名点不动 |
| v0.4.2 | **（完整线）** 修复改名点不动（名字药丸不在可点区域，鼠标被 HTTRANSPARENT 穿透给桌面）；新增 3 项改名回归测试 |
| v0.2.13 | **（无万能键线）** 同 v0.4.3 |
| v0.4.3 | **（完整线）** 第一次修按钮四角；轮盘毛玻璃调强；对话框毛玻璃收敛；桌面新增分发版 |
| v0.2.14 | **（无万能键线）** 同 v0.4.4 ⚠️ 毛玻璃有 bug |
| v0.4.4 | **（完整线）** **分辨率 / DPI 适配**：每显示器 DPI 感知 v2、统一缩放系数、「界面缩放」设置 ⚠️ 毛玻璃有 bug |
| v0.2.15 | **（无万能键线）** 同 v0.4.5：修复毛玻璃错位与淡出 |
| v0.4.5 | **（完整线）** 修复 v0.4.4 的两个毛玻璃 bug（物理像素画进逻辑坐标系、模糊底没乘淡出 alpha）；新增淡出回归测试 |
| v0.2.16 | **（无万能键线）** 同 v0.4.6 |
| v0.4.6 | **（完整线）** 按钮四角脏块根治（SupportsTransparentBackColor + OnPaintBackground）；对话框改浅色实底；轮盘毛玻璃更透；署名 `by exper7` + BETA；改名限长 12 字 |
| v0.2.17 | **（无万能键线）** 同 v0.4.7 |
| v0.4.7 | **（完整线）** 手感九轮打磨：动画收尾闪现 / 把手交叉淡入 / 截图流程提速 / 气泡开关 / 展开收起速度分开调 / 关闭键重做 + 「后悔」机制 / 长按提示条不再被万能键遮住 / 正式更名**快照轮环** |
| v0.2.18 | **（无万能键线）** 同 v0.4.8 |
| v0.4.8 | **（完整线）** 体验 8 项 + 工程 3 项：**默认展开**（不再默认收起）、万能键四分区可自定义、收起把手用途提示、长按出盘 260ms→140ms、拖出默认只给图片格式、设置高级项折叠、新手引导改场景化（"截完直接拖进微信"）、启动检查更新；毛玻璃抓屏移到后台线程（BelowNormal，否则抢 CPU 反而更卡）、一键安装器、自签名流程。修掉 4 个「测试全绿但行为错」的 bug：点设置确定轮盘消失、右键删除不落盘、连点删除两张都删不掉、**万能键动作名改完不变**（根因：设置窗口每次打开读默认值，确定后把改动打回原形 —— 改为确定时强制落盘）。收尾再加**帧耗时统计**（一帧 >25ms 就把那 10 秒的慢帧数与当时界面状态写进 error.log 的 `[Frame]` 行，用来定位"点按慢半拍"）+ **行为测试 6 项**（存盘往返 / 删除落盘 / 删除动画走完真删 / 连点删除 / 设置确定后轮盘还在 / 万能键动作真执行，四个老 bug 逐个注入验证"退回旧代码就会被抓"）；测试改用临时目录，不再重写用户真实的 %APPDATA%\SnapWheel |
| v0.2.19 | **（无万能键线）** 同 v0.4.9 |
| v0.4.9 | **（完整线）** **源码拆分**：6,600 行单文件 → `src\` 下 19 个文件（WheelForm 拆成 5 个 partial：主窗口 / 绘制 / 动画 / 毛玻璃 / 输入），`build.ps1`、`record-version.ps1`、`发布新版本.bat` 全部改为编 `src\*.cs`；**管理员权限下的拖放引导**（拖不动的那一刻当场弹说明 + 一键以普通权限重启，不再只靠一条很多人关掉的开机气泡），并补上行为测试；**GitHub Actions CI**（push / PR 自动编译两条线 + 跑五套测试）。产物大小与拆分前完全一致、五套测试全绿 |
| v0.2.20 | **（无万能键线）** 同 v0.5.0 |
| v0.5.0 | **（完整线）** 两个功能大件：**贴图到屏幕（图钉）** —— 缩略图上**中键**单击，把这张图钉在屏幕任意位置：滚轮缩放（以光标为锚点）、拖动移动、双击 / Esc 关掉，托盘菜单一键收掉全部贴图；**截图标注四件套** —— 截图浮层上加一条工具条（**箭头 / 方框 / 马赛克 / 文字** + 四色 + Ctrl+Z 撤销，快捷键 A / R / M / T / V），马赛克是真把底图糊掉而不是盖块灰，确认时标注用同一个变换合成进图片（工具条、选框、手柄都不会被存进去）。新手引导补上这两条。
**取字（OCR）**：浮层工具条上的「字」把选区里的文字认出来并复制到剪贴板；托盘也能识别剪贴板里的图。
走 **Windows 自带 OCR（Windows.Media.Ocr）**，不打包任何模型/第三方库 —— 中英文都能认（实测中文 87ms）。
顺手把多年没动的性能坑也修了：毛玻璃抓屏原来在 UI 线程同步做（**实测一次 11.7~15.3ms**），
回落到 3.6ms（最慢 5.5ms）。 |
| v0.5.0 修订 | 按用户反馈修：**(1)** 用文字工具时按回车会把整张截图误确认掉（浮层的 KeyPreview 让回车漏到了"回车=确认"那条分支）—— 现在输入中回车只落字，要确认得再按一次；**(2)** 标注文字可切「带白底 / 不带底」（工具条按钮或 `B`，选择记进设置）；**(3)** 醒目的首次引导 —— 第一次用截图浮层时工具条上方亮一条黄色提示，升级后首次启动自动弹一次引导（新功能标【新】），轮盘开机提示"中键=贴图"；**(4)** 顺带自查：点到别处时输入框失焦会丢掉打好的字（已改成失焦即落字）、比屏幕还大的图贴出来会糊满桌面（已自动先缩进屏幕）；**(5)** 开机**一律展开**轮盘（收起态改成"用完自己收"，不再一开机只看到边上一小条）；**(6)** 修 v0.4.8 引入的**恶性 bug「缩略图拖出去放不了」**—— 拖出载荷被砍成只剩图片格式，拖到资源管理器/桌面/只认文件的程序一律放不进去，现在恢复成图片+文件两种格式，老配置按 Rev<3 自动迁移；**(7)** 截图标注的工具条会压住要截的内容 —— 现在优先摆在选区**外面**（下→上→空的多的那侧），鼠标不在附近时自动变淡；「分辨率/角度」面板被选区盖住时会挪到选区外面；**(8)** **取字准确率**：小字一律先放大 2 倍再识别（14~20px 从 25% 提到 88~99%），放大用 GDI+ 高质量双三次；**(9)** 取字改到后台线程（不再冻界面）+ 开机后台热身引擎；**(10)** 启动动画的元素出场曲线换 smoothstep（峰值每帧位移 10.3px → 5.4px，时长/速度一字未动） |
| v0.2.22 | **（无万能键线）** 同 v0.5.2，另外**默认整体小一号**（这条线没有摇杆盘，内圈不用留那么大地方）：缩略图长边 96 → 80、环半径 300 → 250。只影响新配置，已有设置照旧。设置界面同样改成轮盘式四页分页（窗口固定 760×560）。**这是 0.2 线的最终版**，之后 0.2 线不再更新 |
| v0.5.2 | **（完整线）动画性能大优化**：先用**分段计时**把"一帧 12.7ms 花在哪"测出来（清屏 0.2 / 接住区 0.8 / 环 0.5 / 缩略图 6.0 / 控件 5.5 / 推屏 0.4）—— 结论是慢在"一笔一笔重画矢量图形"，**推分层窗口只占 0.4ms**。按数据动手：
**(1) 分层缓存**：环、控件（关闭键 / 设置键 / 万能键 / 名字药丸 / 把手）在**稳态**下整块缓存成位图，每帧只贴一张 —— 用 64 位签名把这一层依赖的状态（几何、风格、强调色、控件悬停/按下…）全算进去，签名一变就重画；alpha 不满、动画中、有悬停按下时自动退回逐帧绘制，画出来完全一致。
**(2) 1:1 贴图绕开重采样**：画布开着 HighQualityBicubic 时，即便 1:1 贴图 GDI+ 也走双三次插值（实测**每张 0.5ms**）—— 现在 1:1 时临时复位变换直接贴。
**(3) 卡片贴片缓存**：阴影 / 圆角玻璃底 / 描边按尺寸+样式缓存成小位图（尺寸稳定才用；尺寸每帧都变时直接画，实测那样反而更快），并按 24px 网格量化，放大预览生长过程中复用同一张。
**(4) 缩略图不再每帧从原图重缩**：优先拿已有的大缩略图当源（这正是"悬停放大"一档的根因），整套基准里的生成次数降到 28 次。
**(5) 换底交叉淡入整帧只混一次**（以前每张卡片各混一遍"旧底+新底"两张全窗口图），并且隔帧重建。
**实测（他真实的 125% 界面缩放、822×822 窗口、三次取最优）**：开启动画 9.1 → **7.9ms**；收起 20.8 → **15.6**；悬停+放大预览 15.9（p95 26.5）→ **14.5（p95 16.1）**；玻璃换底交叉淡入 20.3 → **16.3**；切盘闪光 11.5 → **9.4**；滚动翻图 **10.0** —— **全部每帧 ≤20ms、没有 >40ms 的帧**。
新增 `tests\perf-bench.cs`（不模拟鼠标键盘，反射驱动动画帧并计时，带分段报告）和 `Perf` 分段计时（`SNAPWHEEL_PERF=1` 打开，平时零开销）。
**(6) 设置界面改「轮盘式分页」**：窗口固定 **760×560**，顶部一个小圆弧分四个扇区（点扇区翻页、滚轮也能翻页，当前页高亮成主题色、圆弧里写着页名），四页 = ①行为与快捷键 ②轮盘与外观 ③风格 ④万能键与高级，每页都不滚动；每页的控件**第一次翻到那页才建**（没看过的页不会被"确定"覆盖回默认值），打开设置 **416ms → 140ms**；整个窗口的布局同时改成"坐标明确"（每张格子写死行列数、每个控件 `Add(控件, 列, 行)`），再不会因为改动添加顺序把标题排到最底下（这一步单独做了一版，渲染图与改动前逐字节一致）。**(7) 修遮挡 + 翻页过渡**：设置界面四页里 12 个下拉框的底边和下拉箭头都被**裁掉 4px**（组合框继承窗口字体后真实高度 27px，但它对外报的"首选高度"只有 23px，自动尺寸的行因此矮一截）—— 行容器改成按**子控件的真实底边**兜底，顺带把按钮行里 200×100 的撑宽空 Panel 收成 1×1；切页也不再是"啪"一下换页，改成 **160ms 位移滑动**（新页从一侧滑入、旧页反向滑出，两页全程刚好拼满可视区 —— 不重叠、不留缝；分页器扇区的高亮和凸起用同一条缓动同步过渡），动画期间点扇区 / 滚轮一律忽略（防连点、不会重叠也不会跳变），结束帧与静态布局**逐像素一致**、不留 1px 偏移。**(8) 分页器改成"按住拖动转环"**（和主界面轮盘"能转"的手感对齐）：在圆弧上按住拖动时整环 **1:1 跟手连续旋转**（绘制按 150° 周期分成 ±150° 两份画进弧窗，所以转出去的部分从另一头转进来，圆弧永远是一段完整的四瓣、不缺角不外溢），**转到指位上的那一瓣就是当前页**（转过一瓣换一页，高亮和弧内页名同步）；松手 **ease-out 吸附到离当前角度最近的正角度**（160ms，回弹 ≤75°，结束时角度精确落在目标角、不留偏差）；**位移 < 4px 算点击**（点扇区翻页，行为不变）、**≥ 4px 算拖动**；拖动可以直接接管上一次没走完的过渡（从当前进度接着走，不先跳回去）；鼠标移出窗口、松手在窗口外、捕获被系统抢走、快速来回拖都不会卡在半路（捕获释放即收尾，Dispose 停掉定时器）。**(9) 设置窗口三处实测反馈**：① **标题「SnapWheel 设置」底下被削掉一排** —— Label 的 AutoSize 按字体行高给高度（13pt 粗体只给 22px），而这段文字真正要占的格子是 25px，和上一轮下拉框"报 23px 实高 27px"同一个坑；现在标题自己量真实宽高，上下各留出空白像素行（工具断言：墨迹 20 行 = 自然高度，上下各有一整行背景像素，分页器离标题 8px）。② **翻页滑动时选项文字在闪** —— 根因是滑动靠"每帧挪真控件"：页里的标签/勾选框/下拉框各自都是独立窗口，每帧挪一次就要重画一次（实测一次翻页里内容容器被重排 23 次、页容器连同里面的行容器 25 次）；改成**位图滑动**：切页时两页各拍一张位图（16~34ms）、真控件全部藏起来，由双缓冲的 body 按整数偏移贴这两张图（动画帧之间一次布局都没有），文字是同一份光栅 —— 中间帧与静态图**逐像素差异 0**；窗体另加 WS_EX_COMPOSITED（整窗合成），窗体/页容器/行容器全部双缓冲。③ **分页器"能点 + 能滚 + 能拖"三合一**：滚轮 **一格 = 一页**（向上滚 = 往前一页，和主界面轮盘一致），环跟着转一格（渐变不硬跳）停手后吸附回正角度；动画中滚轮可**直接接管**、拖动中滚轮忽略、点击扇区仍直接切页 —— 三种方式的落点都精确在正角度（≡0 mod 150°）。**(10) 修回"毛玻璃换底看不见过渡"**（用户报"变化时好突兀"）—— v0.5.2 那次"整帧只混一次 + 隔两帧重建"的提速留了三个坑：① `BackdropMix()` 里写的是 `_frameNo - _backdropMixFrame < 2`，但帧号每帧都 +1、淡入进度也是每帧都推进，这个条件**恒为真** → 等于永远贴上一帧（甚至上一轮淡入留下的那张）混图，淡入被量化成两帧一步；② 混图里给 fade 加了下限 `Math.Max(0.06f, …)` → 换底那一帧凭空就是 6% 新底，开头那一下最硬；③ 图层缓存的签名里没有"玻璃底换到第几代"，控件层（万能键玻璃盘/两个圆按钮/名字药丸/把手）的模糊底是烤进位图里的，换底那 0.38 秒会整段不跟着淡、等签名一变再整块跳过去。现在：混图一帧只混一次（同一帧里所有元素共用）、fade 用真值不再加下限、换底时给代次 +1 让缓存失效并且淡入期间两层都不贴缓存（宁可这 0.38 秒老实重画）。**实测**（`%TEMP%\sw-glass.cs`：真路径 `RequestBackdropAsync` → 等新底回来 → 逐帧跑）：过渡 **21~24 帧 / 388~391ms**（改动前同机同工具 12~15 帧 / 384~405ms，且首帧实际画出去的混比是上一轮遗留的混图而不是真淡入帧）；逐帧画面变化比例 0.00→0.15→0.34→0.48→0.67→0.86→1.00 与 fade 同步单调；`perf-bench`（26 张 / 125%）"玻璃换底+交叉淡入" 平均 **12.5ms**、p95 18.0ms、**0 帧 >20ms、0 帧 >40ms**（改动前这一档是 16.3ms）。 |
| v0.5.1 | **（完整线）** **后悔药：撤销删除** —— 删掉 / 一键清空的图不再一去不回。托盘菜单「撤销上一次删除」把最近删掉的那批（一次清空就是整批）放回原盘，最近 8 次删除都记得住，第一次删图时轮盘上会当场说明「还能找回来」。**刻意不做"回收站"**：不建目录、不写索引、不加管理窗口 —— 图本来就在内存里，只多留一份引用就能立刻撤回，退出程序即清空，不会像回收站那样越堆越大。
另外修掉一个一直在偷偷拖慢程序的问题：**窗口关掉后动画定时器没停**（`WheelForm` / `OverlayForm` 的 `Dispose` 只放了位图，15ms 的动画定时器还在后台空转、还在对着已关闭的窗口重绘和重抓玻璃底）。同一个缺陷让行为测试越跑越慢 —— 每多一个没关的窗口就多一份空转，整套测试从 **331.7 秒降到 8.7 秒**。顺带修掉关闭窗口时后台线程糊好的那张玻璃底图没被释放。

**按用户反馈（"工具栏挡住了截图区域"）重做工具条摆放**：以前只在选区上下找位置，选区一高就只能压在截图上；现在四个方向依次试 —— **下方 → 上方 → 右侧竖排 → 左侧竖排**，任一方向放得下就绝不压选区；四处都没空（选区铺满整屏）才允许压住，并且此时画得更透（空闲透明度 165 → 108）；高 DPI 小屏（1080p 开 150% 时竖排会比屏幕还高）会自动**收紧按钮间距**再试一次。还修掉两个相关的老问题：**拖框选的过程中工具条不再显示**（那会儿它会追着鼠标，正好挡在你要选的地方）；**"鼠标离开就变淡"这条逻辑其实从来没接上**（`_toolAlpha` 算了但绘制时写死传 255，图标也没跟着淡）—— 现在真的生效了，而且鼠标一移动就会重绘那一下。新增 2 条布局回归测试（工具条不许和选区相交、拖框选时不显示）。

新加一条用户要的：**截图确认时同时把图复制到剪贴板**（默认开，设置第 1 页「截图后同时复制到剪贴板（要立刻粘贴时直接 Ctrl+V）」可关）—— "截完立刻粘一次"（`Win+Shift+S` 之后 `Ctrl+V`）是最高频的用法，以前截完只在环上、要粘得先从角落把图拖出去，比系统截图慢一步；现在要马上粘就直接 `Ctrl+V`，**而且环上还留着一份**（系统截图不给留）。一次放三种格式：**Bitmap + DIB**（画图 / Word / 微信走这两个）**+ PNG**（认这个格式的程序能拿到带 alpha 的无损原图，不会像 DIB 那样掉透明通道）。剪贴板被别的程序占着时只写一条日志，绝不弹框打断截图。新增 2 条行为测试（①开着时剪贴板里就是这张图、尺寸与选区一致，PNG 解回来尺寸与中心像素也一致，关掉时剪贴板一动不动；②截图确认之后剪贴板监听不能把这张刚截的图又收一盘 —— 一次截图只多一张，外面复制一张**尺寸恰好相同、内容不同**的图照样能收进来）。
**修掉这条功能自己引入的回归：截图后同时复制，轮盘会多收一张（一次截图两张缩略图）**。根因：轮盘一直挂着剪贴板监听（`WM_CLIPBOARDUPDATE` → `OnClipboardChanged`，设置项「复制图片后自动收进轮盘」默认开），截图确认把成品图写进剪贴板之后，**自家的监听把这张刚截的图当成"外面复制的新图"又收了一盘**（App 里本来就有一句 `st.Add(ov.Result)`）。修法：谁写剪贴板谁先在 `SelfClipboard` 登记（**尺寸 + 11 个采样点**的指纹：四角 + 中心 + 四个 1/4 点 + 两个 1/3 点），监听那边先比对，是同一张就**跳过这一次并清掉登记**（只跳过一次，不会长期屏蔽），跳过时顺手记进 `_lastClipFp` 防"同一次写剪贴板被通知多条"。**刻意不用"写完 1.5 秒内忽略"那种时间窗**（原来轮盘双击"复制这张图"就是那么干的）：窗口会把用户在这段时间里真正复制的一张图一起吞掉，而且截图浮层是模态的、消息什么时候被泵到并不确定。顺带把轮盘那条老路径也换成同一套登记，那个 1.5 秒魔法值删掉了。自查过其余路径：OCR/取字只往剪贴板写**文字**（`Clipboard.SetText`，`OnClipboardChanged` 要求 `ContainsImage()`，不可能被自己的文字触发导入），拖出去用的是 OLE 拖放（不经过剪贴板）且本来就有"正在拖出"的保护。
**再修掉同一条功能带来的卡顿：截图确认会卡住"缩略图滑入"**。先量后改（临时计时探针，不进仓库）：一张 1600x1000 的图，PNG 编码 **~35ms** + OLE 把 Bitmap 刷成 DIB **~45ms**，整条写剪贴板的路径在 UI 线程上要 **80ms**（真实流程实测 `Confirm()` 从 81.0ms 涨到 **195.4ms**）；这 80ms 一旦落在动画帧上就是**一帧 50.2ms**，而滑入动画的基线每帧只有 2.4ms（p95 3.4）—— 渲染路径本身没问题，卡的就是剪贴板这一下。两处修法：**(1)** 写剪贴板整件事交给一个**专用 STA 工作线程**（`SelfClipboard.BeginWrite`：OLE 剪贴板只能在 STA 线程上碰），UI 线程只付一次位图拷贝（实测 **~7ms**）—— 这次拷贝必须有：轮盘那几帧正拿着同一张 GDI+ 位图在画缩略图，后台线程再去编码它会直接撞"对象当前正在其他地方使用"（探针里复现过一次进程崩溃）；**(2)** 监听里"这次是不是我们自己写的"改成先用**剪贴板序号**（`GetClipboardSequenceNumber`，O(1)、**连图都不用读**）判断，原来那道"把整张图读回来算指纹"降级成兜底 —— 读一张 1600x1000 要 ~10ms，那一下也落在动画帧上。实测（同一份探针，对着修复前的 src 和修复后的 src 各跑一遍，1600x1000、每种 5 次取中位）：`Confirm()` 的 UI 阻塞 **195.4ms → 77.2ms**（剪贴板那部分的贡献 114ms → **9ms**）；监听跳过自写图 **10.0ms → 0.1ms**；滑入动画每帧最慢 **50.2ms → 10.3ms**（中位 2.7、p95 4.8，**全部 ≤20ms、无 >40ms 帧**）。剪贴板能力一字未减：仍是 **Bitmap + DIB + PNG** 三种格式、仍然 `copy=true` 立刻持久化（程序退出后照样能粘），"只跳过一次"的自写跳过逻辑原样保留（第 44 条测试照旧守着，把那两道跳过都去掉重跑仍会 FAIL）。
**修掉"缩略图滑进 wheel 突然闪现、动画和位置合不上"**（用户接着报的）。逐帧量出来是这样：收起态（用户配置 `CollapseMode=1`）下收起时 `_offset` 被归零，于是第 7 张的 `ItemPhi = 1.747`，已经贴到"可见弧上界 + 0.5"（`_phiMax + 0.5 = 1.766`）里面一点点；而绘制时还有"新图沿弧滑入"的偏移（`phi = ItemPhi + (1-pr)*slide`，收起态展开时 slide 是 0.62）—— 也就是说**滑入动画基本在弧外跑**：得等 `(1-pr) ≤ 0.063` 那张卡才勉强跨进弧里，于是它先隐身一秒多、再"啪"地闪现在弧的边缘（探针逐帧抓：0~540ms 全程"不在可见弧内"、`pr` 卡在 0.00）。两个原因：**①** `MarkNew`（"这张刚截的，现在就滑进来"）没把视口带过去 —— 剪贴板导入那条路一直有 `_targetOffset = Count-1`（`OnClipboardChanged` / `ImportFiles`），截图这条路漏了；**②** `90-App.cs` 里 `MarkNew` 调在 `ExpandWheel` **之前**，而收起态展开走的是 `ExpandWheel → StartIntro`，`StartIntro` 会把 `_enterT0` 清空、重排成"0.45 + i*0.13 秒"的错峰出场表（开机彩虹扫出用的那张），刚截这张的"现在就滑"当场被顶掉 —— 得等 1 秒多才动。现在：`MarkNew` 里补上 `_targetOffset`（视口跟到最新那张），并把 `MarkNew` 挪到"拉出 / 显示"之后。逐帧复核（同一份探针、用户真实几何 Radius=256 / ThumbSize=96 / Slots=5 / 收起态）：`pr` 从 32ms 起就往上爬、78ms 就进弧，DrawnRect 从 (38,343) 平滑走到 (196,507) 停在第一格，不再"隐身再闪现"；出图看过一眼 —— 新截那张（蓝底黄圆）稳稳落在第一格、计数 7/7。新增行为测试第 45 条守着这两点（视口跟最新 + 200ms 内必须已经在滑），把 `_targetOffset` 那句去掉重跑会 FAIL（"视口没跟到最新那张：_targetOffset=0，新图索引=6"）。 顺手做的两处加固：**出错日志加上限**（超过 512KB 转存成 `error.log.1`，只留一代；之前只增不减，挂久了能涨到几 MB）；**翻译的错误提示分类**（限流 / 接口报错 / 空返回 / 不是 JSON 各给各的人话原因，超时 9 秒 → 15 秒，长文分段后不再被一次抖动全判失败）。 |
| v0.5.3 | **（完整线）这一版四件事：设置界面可「有限度」自由缩放 · 其余所有对话框（新手引导 / 管理员提示 / 取字翻译 / 多轮盘管理 / 贴图）在 150% DPI 下“显示不全”一并修掉 · 缩略图进轮盘改成"从弧起点往上摞、堆满后新图从上面挤进来把旧的往下挤一格" · 新增设置项「截图后把滚动位置重置到最新那张」（默认开）**（用户计划第 1、2 条）。
**(1) 设置窗口可缩放 + 默认尺寸按 DPI/内容反推 + 记住你拖过的大小**：以前是固定 `FixedDialog` 760×574，选项越加越多之后有用户反映"有些选项的字显示不全"，高分屏（150% = 144DPI）下更严重 —— **字号按 DPI 放大 1.5 倍、窗口却一动不动**，内容顶出可视区（底部按钮行和页脚被压住、下拉框文字被截）。现在三件事一起做：**① 长度全按 DPI 缩放**：`K = 屏幕真实 DPI 系数`，窗口默认尺寸/`MinimumSize`/四行行高(标题 28 / 分页器 96 / 按钮行 40 / 页脚 32)/内边距/按钮与数值框尺寸/下拉框宽度/行距/分页器弧线几何**全部乘 K**；**字体的点数一个都不动**（点数本来就跟着 DPI 走，再乘一次会变成 2.25 倍）—— 圆角也改成按高度算，否则按钮被放大后圆角跟不上。**② 默认尺寸不再写死**：`默认客户区 = max(出厂尺寸 × K, 这一页的"内容首选尺寸" + 固定行高 + 内边距)`；页面里的字号已经是按真实 DPI 渲染的，所以量出来的首选尺寸天然含 DPI 系数。第 1 页在打开时就量（不够就把窗口长大），**其余各页第一次翻到时就地量**（`BuildPage` → `EnsureFit`，只长大不缩小）—— 这样"每页懒建"没被破坏（实测四页全量要多花 ~130ms），但**任何一页被显示出来时都已经按内容补足过尺寸**，不会裁。**③ 上限 = 屏幕工作区的 92%**（本机 2560×1600 工作区 → 2355×1472，用 `MaximumSize` 夹住，拖不出去、也长不出屏幕）。放大后**多出来的高度全给"当前页"那一格**（root 第 2 行是 `Percent(100)`），固定行高不跟着错位；拖边框时会把翻页滑动用的那两张位图先精确收尾、再让当前页按新尺寸重排。**用户拖过的尺寸会记住**：关闭设置窗口时把客户区写进 `settings.ini` 的 `WinW/WinH`（和打开时不一样才写），下次打开就用它、并夹进 `[最小, 最大]`（换到别的缩放比的屏幕上也绝不会小于内容）。**四页布局仍然"坐标明确 + 保存只写建过的页"**（这两条是这个窗口的保命规则，见 `75-SettingsForm.cs` 顶部）。**验收**：一次性工具（`%TEMP%`，不进仓库）在**等效 100% / 125% / 150%** 三档 DPI 下各出四页图并量"每个子控件 vs 页面格"，**三档都一处不裁**；125% 等效下公式默认 = 950×718（内容 638×336 ≤ 页面格 900×433）、150%（本机真实）下 = 1140×861（内容 986×436 ≤ 1080×519），真机默认尺寸下把四页走一遍：**不裁、窗口也没被撑大**；存过 `1180×921` → 打开就是 `1180×921`，存过 `100×100` → 被夹到最小 `1140×861`，没存过 → 用默认。出图逐张看过：四页文字完整、底部「确定/取消」与页脚 `SnapWheel v0.5.3 · by exper7 · BETA` 完好、分页器没被压。
**(2) 缩略图进轮盘的堆叠逻辑**（用户原话："截图后产生缩略图显示的永远是刚截下来的缩略图，这就导致之前的缩略图在下面全都显示不全，而 wheel 上半部分又空空的没有利用上，我希望缩略图进 wheel 的逻辑是**从 wheel 上端一个个滑下来直到堆满，再让新出现的开始从上面挤进来，旧的就被挤下去**，同时也就是说截完图后的 wheel 滚动状态需要在截完图后重置（我希望滚动状态是否要在截图后被重置以显示动画可以作为选项在设置里被调整）"。
**根因**：`_offset` 的含义一直是"**落在弧下端那一格（`_phiMin`）上的图片下标**"，而"收新图后跟到最新"用的是 `_offset = Count-1` —— 也就是**最新那张永远落在弧的*下端*那一格**。于是每截一张，之前的图全被推到弧下方（`ItemPhi < _phiMin`：第 2～3 张还能看见半张被窗口边切掉、第 4 张起整张画不出来），而弧的上半截（`_phiMin.._phiMax`）从头到尾空着。
**改法**（只动"锚点"，`ItemPhi` / 绘制 / 命中测试一行没改）：跟到最新 = `_offset = Math.Max(0, Count - Slots)`。**没堆满（Count ≤ Slots）时锚在弧起点**（`_offset = 0`）→ 第 0 张贴住 `_phiMin`、往上一格一格摞，新图一律**从弧上端滑下来**（`EnterSlide`：起点取 `_phiMax + 0.10`，所以它自己的格子越低滑得越远），于是"容器"是从下往上渐渐装满的、下面几格不留空；**堆满（Count > Slots）**时 `_offset = Count - Slots` → 最新那张顶在弧上端，再来新图时 `_offset` 整体 +1，即"**新图从上面挤进来、老图一起被往下挤一格**"，最下面那张滑出可见弧。视口可滚范围是 `[Max(0,Count-Slots), Max(0,Count-1)]`；`RefreshWheel` / 切盘 / 窗口构造 / 四种"收进新图"的路都走同一个 `OffsetForNewest()`，不会各走各的。
**逐帧探针**（`%TEMP%`，不进仓库；用户真实几何 Radius=256 / ThumbSize=96 / Slots=5，`_phiMin=0.305` `_phiMax=1.266` `StepRad=0.240`，连喂 7 张）：`Count=1/2/3/4/5` 时第 0 张的 `ItemPhi` 恒 = `0.305`（=`_phiMin`，贴住弧起点），新图**首帧都在弧上端或更外**（绘制 phi=1.366 / 1.566）、之后**逐帧单调下滑**到这一摞的最上一格，**滑行距离随摞高递减：1.061 → 0.821 → 0.581 → 0.340 → 0.300 rad**（前几张一路滑过整段弧，越摞越高、滑得越短 —— 就是"往容器里放"的手感）；`Count=6/7` 时**每一张旧图的位移恰好 = -0.240（往下挤一格，误差 <0.005）**、被挤出去的**最下面那张 `ItemPhi=0.064 < _phiMin=0.305`**、最新那张落位 = `_phiMax`。**26 条探针断言全过**；每帧 4～10ms（首帧 15～22ms 是 JIT/缓存预热）。4 张关键帧 PNG 逐张看过：1 张时那张贴在弧起点、3 张时 `#1 #2 #3` 自下而上摞起来、7 张时 `#7 #6 #5 #4 #3` 自上而下排开且 `#2` 只剩被挤出弧外的一条边、还有一张第 8 张滑到一半（正从弧上端下来、旧的已经整体下移一格）。（`EnterSlide` 那条公式只在**没堆满**时把起点抬到弧上端；堆满时仍是老样子 0.30 / 开启动画 0.62。）
**(3) 新增设置项**（默认**开**）：第 2 页「截图后把滚动位置重置到最新那张（好让滑入动画看得见）」。**开** = 视口跟到最新那张（`MarkNew` / 剪贴板收图 / 导入文件 / 拖回轮盘这四条"收进新图"的路都走同一个 `FollowNewest()`）；**关** = 一句都不碰 —— 用户的滚动位置和每一张旧图的位置都保持原样，新图仍然从上面滑进来（探针：`_offset` 2.00 → 2.00、旧图位移全 0、新图照样入场落位）。**刻意不占新的一行**：第 2 页再加一行要多 31px，而 760×574（窗口允许缩到的最小尺寸）下页面格只有 346px、内容已经要 324px —— 加一行就顶出去被裁了，所以放在「长按放大(%)」那一行右边的空处。
**(4) 其余所有对话框在 150% 下"显示不全"的全面修复**（用户原话："凡是涉及到界面的都可能有显示不全的bug，比如引导界面，新手引导等"）。第 (1) 条只修了设置窗口，其余窗口（新手引导 / 管理员提示 / 取字翻译 / 打赏 / 多轮盘管理 / 贴图）仍是写死像素 —— 150%（144DPI）下"长句右半截被裁、说明文字只剩一行、按钮被挤出窗口"就是这个。这一版把它们统一到同一套规矩：新增 `src/13-Ui.cs`（`Ui.K` 取真实 DPI 系数并夹在 `[1,3]`；`Ui.S / Ui.Sz / Ui.Pt / Ui.Pad` 缩放长度；`Ui.Wrap(Label, maxW)` = `AutoSize + MaximumSize(maxW, 0)`，让文字**自己折行、自己报 PreferredHeight**；`Ui.TextH` 量文字在给定宽度下的高度），**字体磅值一律不乘**（GDI+ 已按 DPI 渲染过一次，再乘就是双倍放大）。逐窗口的病根与改法：
- **新手引导 / 管理员提示**（`80-Dialogs.cs`）：10 条说明原来全是写死 `486×20` 的死格子 → 全部改成 `Ui.Wrap` + `PreferredHeight` 流水线排版。**顺带发现一个更危险的隐患**：内容按 DPI 长高后，150% 下引导窗口约 1500px、**比屏幕工作区还高**，底部「开始使用」会被顶到屏幕外面 —— 这不是"裁字"而是"整个按钮看不见"。现在窗口高度夹到工作区以内，装不下的部分用**滚轮整块翻**（新增 `UiScroll`：只把子控件整块上下挪，**不用** `Panel/AutoScroll`，否则不透明的 Panel 会盖掉窗口的半透明毛玻璃底），底部按钮与滚动提示**固定贴底**，滚到哪儿都点得到。
- **取字 / 翻译结果**（`81-OcrForm.cs`）：标题、副标题、状态行原来 `AutoSize` 但不封宽 → 被窗口右边沿切掉；两个文本框的宽度用 `ClientSize.Width - 48`（那个 48 不跟 DPI 走）；4 个按钮是死尺寸，而 `RoundButton` 的字是 `TextRenderer` 直画进按钮矩形的、**不够就硬裁** → 改成 `BtnW/BtnH = max(设计值 × K, 实测文字 + 内边距)`；窗口最后按内容重算（150% 下 620×560 → 620×840），一屏放不下时**只压那两个带滚动条的多行框**（下限 `Ui.S(80)`），标题/说明/按钮一个都不压。
- **多轮盘管理 / 重命名**（`70-WheelsForm.cs`）：列表 `ItemHeight` 写死 26 是"每行文字上下被切"的直接元凶 → 过 `Ui.S`，自绘的色点与文字改成**在行高里居中**（不再写死 `+7 / +5`）；底部提示是 `260×22` 死格子、且位置靠"右列肯定更矮"的巧合才没被压住 → 改成 `Ui.Wrap` + 动态 y；重命名窗口的说明文字原来是无上限的 `AutoSize`（只保证不裁字、不保证不出窗口）。
- **贴图窗口**（`55-PinForm.cs`）：这个窗口一个字都没有，不存在裁字；只把 × 按钮、留白、笔画粗细过 `Ui.S`，并让"按钮尺寸"与"显示门槛"同尺度（否则 150% 下 33px 的按钮会盖满一张 45px 的小贴图）。**图片显示尺寸一个数没动** —— 那是内容像素，乘了图就画错。
**验收**：两条线编译 **0 警告**；离屏几何探针（`%TEMP%`，不进仓库，只建 Form 不显示）在 100% / 150% / 200% 与矮屏下核对"无重叠、内容高 ≤ 窗口高、窗口不超屏"，其中取字窗口在 100% 下与原设计差 ≤1px（`sub=46`、`l1=74` 完全一致）。**没有跑**自动化测试（用户手测），也没有弹任何窗口。
**行为测试 45 → 48 条**：第 45 条（v0.5.2 那条"刚截的图必须跟手滑进来"）的落点断言从"停在 `_phiMin`（下端）"改成"停在 `_phiMax`（上端）"、视口断言从 `Count-1` 改成 `Count-Slots`；新增 46「堆叠：最新顶在弧上端、旧的依次往下，堆满后整排被挤下去一格」、47「逐帧：先隐身于弧外 → 进弧那一刻正好在弧上端 → 一路平滑下滑到位」、48「开关关掉：视口一动不动、旧图一张都不动、新图照样从上面滑进来」。**把锚点退回老逻辑（`Count-1`）重跑，这 3 条一起 FAIL**（"视口没跟到最新那张：`_targetOffset=6`，期望=2" / "第 4 张没顶在弧上端：phi=0.305，_phiMax=1.266" / "它不是从弧上端进来的：首次可见处 绘制phi=0.717"），说明这几条断言是真的在守这件事。
已知形态（不是 bug）：弧上端本来就在屏幕边上，所以新图入场的那 1～2 帧会被窗口左边切掉一部分 —— 看起来就是"从屏幕角里滑出来"。 |
五套测试全绿（几何 ALL PASS / 格式 52-0 / 绘制 150-0 / 绘制无万能键 149-0 / **行为 48-0**，两条线编译 **0 警告**）；性能基准 `perf.exe 26 125` 与同一时刻的 v0.5.2 源码基线 A/B 对照（各 3 轮）：滚动翻图 **p95 10.6 / 11.0 / 14.6ms、最慢 17.1、>20ms 0 帧（达标）**，基线是 p95 26.7～37.1；开启动画（含缩略图滑入）平均 15.4～16.2、p95 21.1～22.5、最慢 24.9、**>40ms 0 帧**，基线是平均 22～25、p95 31.5～33.1、还出现了 >40ms 的帧 —— **每一档都比基线快**。这台机器当时后台有 Edge/Defender，整体比写 v0.5.2 那次数值（开启动画 7.9ms）慢约一倍，所以"每帧 ≤20ms"要按同刻基线对照来看。 |
| v0.6.0 | **（完整线 · 进行中）滚动长截图（手动滚动 + 无缝拼接）· 翻译换成免费引擎链（机翻腔明显减轻）· OCR 准确度提升（低对比度场景 0% → 97%）。** 弧线胶囊与三个小按钮的弧度排布还在做，做完再发这一版。
**(1) 滚动长截图：用户自己滚，程序跟着无缝拼接**。托盘菜单「滚动长截图…」。**刻意不给别的窗口发合成滚轮消息**（那样既要猜滚几格、又要处理惯性和滚动节流，还挑窗口），也不用自动滚动 —— 用户拍板的就是"他手动滚、我负责拼"。进入后屏幕顶部出现一条提示条（已接几段、长图现在多高、失配时直接说原因），**每 200ms 抓一屏**交给拼接引擎判：接得上就把新露出的行追加到画布下方，接不上就等下一帧；Enter 结束出图、Esc 取消，成品直接进轮盘（和普通截图一样带滑入动画）。三个工程细节：**① 提示条设了 `WDA_EXCLUDEFROMCAPTURE`，抓屏拍不到它自己**（否则每帧都带着这条黑边、长图会一路重复），而且它只在第一帧的顶部出现、后续帧只贴新增的底部行，双保险；**② 抓屏用一张复用的位图**，不每 200ms 分配一次全屏（16MB）；**③ 抓的是虚拟屏**，和 App 传进来的第一帧同一坐标系，否则 Push 里的尺寸检查会把每一帧都判成"画面尺寸变了"。
拼接算法：拿上一屏**底部 120 行**当模板（避开最底 8px 的滚动条圆角、右侧 24px 的滚动条），在新屏里沿竖直方向搜偏移 d，判据是「差异像素比例 × 100 + 平均灰度差」的代价。**用合成长页（800×3000，每 100 行一条横线 + 随机彩色块，刻意做成有周期性、最像真实网页）逐帧验证**：正常滚动 5 帧（300 / 340 / 260 / 400 / 400 行）**全部接对**，最终 2300 行与页面真实内容**逐像素平均差 0.000、最大差 0**；一次滚 1200 行（超过可检测上限）被**正确拒绝**并提示"滚过头了，慢一点再滚一下"。
调参过程里修掉两个真 bug（都是"注释写了、代码没做"）：**① 置信度判据里的"次优"取错了** —— 原来扫的时候顺手记次优，可相邻偏移（d±1、d±2…）的分数天然几乎一样，"次优"永远只比最优差一点点，于是**每一帧都被判成"重叠区不够独特"**（实测五帧全被拒、长图停在第一屏高度）。现在第二遍扫描**排除最佳附近 ±24 行**再取次优。**② 模板带太厚** —— 能检测的最大滚动量 d 受"模板带顶行离屏幕顶的距离"硬限制（模板每一行都要能在新屏里找到 y-d ≥ 0），带取 240 行时只能搜到 ~240 行，而人滚一次常常就是 300~400 行，于是永远找不到正确偏移、只能挑到某个局部最优（实测挑出 189、160 这种数，拼出来的图是错的）；带改成 **120 行**后 bandTop 抬到 472，一次滚到 470 行还认得出来。另外判据从单纯"平均灰度差"改成**由"差异像素比例"主导**：周期图案（表格线、列表项）对齐时线条能对上、格子里的字对不上，平均差被大片相同背景稀释（滚过头那次只有 4 点几）照样溜过去，而"明显不同的像素"会成片出现 —— 真匹配只有 ~0.1%（抗锯齿边缘），假匹配 2% 起步。
**(2) 翻译：换引擎链，机翻腔明显减轻**。原来只有 MyMemory（本质是翻译记忆库：质量参差 + 老限流），用户原话是"翻译后机翻过于严重"。2026-09-14 在本机实测同一句英文 —— MyMemory：「加载资源**捆绑**包失败。请确保应用程序**未**在兼容模式下运行。」；有道：「加载资源包失败。请确保应用程序**没有**在兼容模式下运行。」。顺带实测：腾讯 transmart 返回空、**Google gtx 直接 429（国内确实不通）**、豆包 / DeepSeek 的官方 API **都没有免 key 的**（都得注册），所以做成三层引擎链：**① 用户自填的 OpenAI 兼容接口**（填写 URL + key + 模型名；DeepSeek / 豆包 / 通义 / 本地 Ollama 都兼容，URL 只填到 `/v1` 会自动补 `/chat/completions`）—— 质量最好，也真正消掉机翻腔；**② 有道 aidemo**（免费、无 key、国内直连）—— 默认走这层；**③ MyMemory** 保底。① 填了但失败**不会让功能整个不可用**：记下原因继续往下退，三层全失败才报错、且优先报"配置问题"。译文最后过一道 `Tone()` 去机翻腔（中英标点归位、中文之间多余空格删掉、行尾空格与多余空行清理、脱掉引擎自己加的包裹引号），**而且刻意做得很保守**：只把"被中文字夹着"的半角标点改成全角，`v0.5.3`、`1.5`、`http://api.example.com/a.b?x=1` 里的点**一个都不动**（专项验证过）。设置第 4 页新增「翻译接口 / 模型 / API Key」两行，**留空就用内置免费接口**（key 明文存在本机 `settings.ini`，不上传任何地方）。
**(3) OCR 准确度：先修判据，再做预处理**。**①** 那段"字太小就放大再认一遍"的注释写着"判据两条，缺一不可"，**代码里其实一条都没实现** —— 无条件放大 2 倍、再把放大结果无条件当答案，量到的字高 `wordH` 声明了却从没读过。现在真的按量到的字高决定倍数（<12px → 3 倍、<18 → 2.5、<26 → 2、更大 → 1.5），并且**择优**（放大那份只有认出的字更多才采用）。**② 新增低对比度拉伸**：取亮度直方图分位做线性拉伸，只在"确实偏灰"时动手（跨度 <200 且 ≥16），**不做二值化**（会把抗锯齿边缘咬碎、引擎反而更容易认错）。这里踩到一个真 bug：**一开始用 2% 分位，而低对比度图里绝大多数像素都是背景色 —— 往下数 2% 就已经落到背景上了，于是 hi 等于 lo、跨度算成 0，被判成"几乎是纯色、拉也白拉"而永远不拉伸**；改取 **0.2% 分位**后才真正动手。**实测（合成图 + 字符级命中率）**：灰底浅灰字（亮度 84~110）**0.0% → 92.4%**（拉伸 1x）→ **97.0%**（拉伸 + 3x）；白底黑字 14px **82.9% → 87.8%**（2x 起就到平台，所以倍数不必更大）；深色主题黑底白字 14px 90.9%。**说实话**：Windows 自带 OCR 对 14px 中文的天花板大概就在 88% 上下（错的多是"滚动→氵动"这类形近字），要再往上得换第三方引擎（要带模型、体积大一大截）—— 那是产品决策，不在这版里偷偷换。
**(4) 弧线设计语言：胶囊随弧弯成圆弧、三个小按钮沿弧排布**（用户原话："胶囊随弧线弯成圆弧，靠着 wheel 摆放（注意文字的处理），三个按钮围绕万能键的左上方弧形排布，靠着万能键左上方摆放"）。新增 `src/69-ArcUi.cs` 收拢共用几何：极坐标换算、**弧形胶囊轮廓**（沿半径扫一段弧、两端半圆头；用多边形近似，比拿 `GraphicsPath` 拼两段弧加两个半圆稳得多）、**沿弧文字**（每字按自己所在的角度旋转，让"字顶朝外"；旋转角的推导写在代码注释里，并用四个方位的测试图确认过"正上方那一行是正立的、正右那一行立起 90°"）。三件事：**① 三个小按钮**（截图 / 设置 / 收起）从"贴着屏幕角往下排一竖列"改成**沿一段同心弧排布**。这里有个几何坑：弧是绕屏幕角的同心圆，**从万能键出发沿弧走只有"更右上"和"更左下"两个方向，永远到不了左上方** —— 左上只有靠**更大的半径**（更远离角）才能到达；所以半径取 `EffR + thumb×0.75 + 按钮半径 + 6`（正好落在缩略图弧外侧、不压缩略图），角度以万能键所在角为中心左右各摊一档，三个按钮于是"围绕万能键左上方"排成一小段弧。**② 名称胶囊**和 **③ 计数胶囊「3 / 8」**都从"横着的圆角矩形 + 横排文字"改成**沿同心弧弯出来的弧带 + 逐字沿弧旋转的文字**，主题色圆点也挪到弧的端头；两个胶囊分居弧的两端（一个 +0.45 rad、一个 −0.45 rad），中间那段"万能键左上方"正好留给三个按钮，谁也不压谁。**验收**：几何探针（`%TEMP%`，只构造窗口、**不显示**）在左下 / 右下两种角下量出万能键与三按钮的矩形，确认三按钮整体位于万能键左上方、且全部落在窗口内；再用离屏出图逐版看图（第一版胶囊跑到窗口外、第二版把角度收到 0.45 rad 后归位）。
**修订（用户看过离屏出图后指出，当天修完）**：**① 三个小按钮排错了地方** —— 我把它当成"绕屏幕角的同心弧"，于是排到了缩略图弧那一侧，看着是"靠着 wheel"而不是"靠着万能键"。现在改成**以万能键自己为圆心**的一小段弧：半径 = 万能键半径 + 按钮半径 + 6（紧贴键的外缘），方位固定在屏幕左上方（225°），三个按钮在它两侧各偏 40°，真正的"围绕万能键左上方"。**② 名称胶囊不该动** —— 用户原话"项目名字不用变位置"；已**完全恢复原样**（要随弧弯的是「几 / 几」那个计数胶囊，不是它）。**③ 滚动长截图在真实屏幕上"压根用不出来"** —— 根因：模板带取的是**屏幕最底部的 120 行**，而那里正是**任务栏**，它在抓屏里是静止的、根本不跟着页面滚动走，拿它当模板"怎么对都对得上"，匹配必然失败或挑到假偏移（合成长页测试里没有任务栏，所以没暴露）。现在模板带挪到**屏幕高度的 72% 处**（带高同时从 120 降到 100，于是单帧可检测的滚动量从 0.62 屏提到 0.72 屏，滚 700 行也接得上），并新增**第二段验证带**（屏幕 28% 处）：同一个偏移必须在两段互不相邻的画面上都对得上才算数，专门拆穿网页里到处存在的周期对齐全；判据阈值按真实场景放宽（真实滚动有子像素重采样、光标闪烁、视频动画，前后帧不可能逐像素相同）。**回归**（合成长页 800×4200）：5 帧 300/340/260/400/400 行全接对、**逐像素平均差 0.000 最大差 0**；一次滚 700 行也接对。提示条同时写清"把鼠标放到要滚的窗口上，往下滚"（原来那句"滚到哪儿它接哪儿"没告诉用户该滚哪里）。 ← **当前版本** |
</details>
